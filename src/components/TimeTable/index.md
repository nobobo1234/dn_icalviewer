Zorg ervoor dat je een `calendar` prop meegeeft aan de TimeTable die geparset is met de `node-ical` library. De TimeTable zelf doet de rest van het werk.

```jsx static
<TimeTable calendar={calendar} />
```
